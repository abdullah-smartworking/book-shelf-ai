{
  "name": "@bookshelf/mcp-server",
  "version": "0.1.0",
  "private": true,
  "description": "Read-only MCP server exposing BookShelf's JSON data as tools for Claude Code. Dev tool, not production code.",
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bookshelf/shared": "*",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
